# Руководство по использованию GophKeeper

## Быстрый старт

### 1. Запуск сервера

```bash
# С SQLite (по умолчанию)
./bin/server

# С PostgreSQL
./bin/server --dsn "host=localhost user=postgres password=postgres dbname=gophkeeper sslmode=disable"
```

### 2. Запуск клиента

```bash
./bin/client --server localhost:50051
```

## Работа с клиентом

### Вход в систему

1. При первом запуске вам нужно зарегистрироваться
2. Введите логин и пароль
3. После успешной регистрации вы автоматически войдёте в систему

### Главное меню

После входа вы увидите главное меню с опциями:

- **📋 Список данных** - просмотр всех сохранённых данных
- **➕ Добавить данные** - создание новой записи
- **🔄 Синхронизация** - синхронизация данных с сервером
- **🚪 Выход** - выход из приложения

### Навигация

- **↑/↓ или k/j** - перемещение по меню
- **Enter** - выбор пункта меню
- **Esc** - возврат в предыдущее меню
- **q** - выход из приложения
- **r** - обновление данных (в списке данных)

### Типы данных

GophKeeper поддерживает следующие типы данных:

1. **Логин/Пароль** - для хранения учётных данных
2. **Текст** - для произвольных текстовых данных
3. **Бинарные данные** - для файлов и других бинарных данных
4. **Банковская карта** - для данных банковских карт

### Метаданные

Для каждого типа данных можно указать метаданные:
- Принадлежность к веб-сайту
- Связанная личность или организация
- Список одноразовых кодов
- Любая другая текстовая информация

### Безопасность

- Все данные шифруются на клиенте перед отправкой на сервер
- Сервер хранит только зашифрованные данные
- Используется AES-256-GCM для шифрования
- Пароли хешируются с помощью bcrypt
- JWT токены для аутентификации

## Командная строка

### Сервер

```bash
./bin/server [опции]

Опции:
  -port string
        Server port (default "50051")
  -dsn string
        Database connection string (default: SQLite)
  -addr string
        gRPC server address (overrides port)
```

### Клиент

```bash
./bin/client [опции]

Опции:
  -server string
        Server address (default "localhost:50051")
  -v, --version
        Показать версию и дату сборки
```

## Примеры использования

### Регистрация нового пользователя

1. Запустите клиент
2. Введите логин и пароль
3. Нажмите Enter

### Добавление данных

1. Войдите в систему
2. Выберите "➕ Добавить данные"
3. Введите название
4. Выберите тип данных
5. Нажмите Enter для сохранения

### Просмотр данных

1. Выберите "📋 Список данных"
2. Используйте стрелки для навигации
3. Нажмите Enter для просмотра деталей

### Синхронизация

1. Выберите "🔄 Синхронизация"
2. Данные автоматически синхронизируются с сервером
3. Нажмите r для повторной синхронизации

## Устранение неполадок

### Ошибка подключения к серверу

- Убедитесь, что сервер запущен
- Проверьте адрес сервера: `./bin/client --server localhost:50051`
- Проверьте файрвол и сетевые настройки

### Ошибка базы данных

- Для SQLite: убедитесь, что есть права на запись в директорию
- Для PostgreSQL: проверьте подключение и права доступа

### Проблемы с аутентификацией

- Проверьте правильность логина и пароля
- Токены автоматически обновляются при необходимости

## Безопасность в продакшене

⚠️ **ВАЖНО**: Перед использованием в продакшене:

1. Измените `JWT_SECRET` в переменных окружения
2. Используйте PostgreSQL вместо SQLite
3. Настройте TLS для gRPC соединений
4. Используйте сильные пароли
5. Регулярно делайте резервные копии базы данных
